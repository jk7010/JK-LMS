import React from "react";

const Dashboard = ({props}) => {
  return <div style={{ backgroundColor: '#e8f5e8', minHeight: '100vh', padding: '20px' }}> calling from Dashboard   {props}</div>;
};

export default Dashboard;
